{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MultiClawProtocol default capabilities — least privilege",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-exists",
    "dialog:default",
    "dialog:allow-open"
  ]
}
